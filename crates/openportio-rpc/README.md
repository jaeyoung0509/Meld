# openportio-rpc

gRPC integration crate for Openportio.

Includes:
- protobuf definitions (`proto/`)
- tonic/prost generated bindings
- `grpc-docgen` binary for descriptor-based contract artifact generation
